<link href="https://fonts.googleapis.com/css?family=Indie+Flower" rel="stylesheet">

<div class="container-fluid expediente">
    <div class="row  p-5">
        <div class="col-xl-2 col-lg-2 col-md-4 col-sm-12 col-12   ">
        </div>
        <div class="col-xl-10 col-lg-10 col-md-4 col-sm-12 col-12   ">
            <h1 class="tit-txt-expediente">Expediente</h1>

            {{#if appointments}}
            <li>
                {{#each appointments}}
                <ul><a href="/expediente/{{_id}}">{{date}}</a></ul>
                {{/each}}
            </li>
            {{/if}}

            {{#each appointment}}
            <h3>Prescripción:</h3>
            <p>{{prescription}}</p>
            <h3>Notas:</h3>
            <p>{{notes}}</p>
            {{/each}}

            <a href="/expediente">Regresar</a>
        </div>
    </div>
</div>

<script>
    var session = true;
</script>